# TOWN114 콘텐츠 운영 지침

작성일: 2026-07-14 · 애드센스 승인 준비 과정에서 확정한 운영 원칙. 승인 이후에도 유지한다.

## 1. 시각 자료 (다이어그램·사진) 점진 추가

사이트에 이미지가 0개였던 상태를 지역 가이드 5종의 자체 제작 SVG 다이어그램으로 보강했다.
이후 주요 가이드 글 10~20개에도 같은 방식으로 시각 자료를 점진적으로 추가한다.

원칙:
- **자체 제작만 사용한다.** 인라인 SVG 다이어그램(확인 순서, 비교 흐름) 또는 직접 촬영한 사진만 쓴다.
  검색으로 받은 이미지, 타 사이트 캡처, 출처 불명 이미지는 저작권 위험이 있으므로 절대 넣지 않는다.
- SVG에는 `role="img"`와 `aria-label`을 반드시 넣고, `<figure>`/`<figcaption>` 구조를 유지한다.
  기존 예시는 `regions/*/index.html`의 `.guide-figure` 블록 참고.
- 사진을 쓸 경우 `assets/`에 저장하고 파일명을 내용 기반으로 짓는다(예: `jeonju-hanok-parking-lot4.jpg`).
  얼굴, 차량 번호판 등 개인정보가 식별되는 부분은 게시 전 가린다.
- 추가 우선순위(트래픽·검색 의도 기준):
  1. public-parking-guide (주차 요금 구조 다이어그램)
  2. late-night-public-pharmacy-guide (야간 약국 확인 흐름)
  3. bulky-waste-online-report-guide (배출 신고 절차도)
  4. moving-address-checklist (이사 순서 타임라인)
  5. community-center-guide, pharmacy-weekend-guide, ev-charging-station-guide 순으로 확장
- 한 번에 몰아서 넣지 않고 **주 2~3개 글**씩 추가한다. 급격한 일괄 변경보다 꾸준한 갱신 이력이 낫다.

## 2. 자동발행 운영 규칙

2026-07-14에 자동발행 글 37개 중 20개가 "같은 내용, 날짜만 다른 재발행"으로 확인되어 삭제했다.
원인은 `scripts/auto-add-post.mjs`의 폴백 로직(미사용 주제가 없으면 이전 주제 재사용)이었고,
같은 날 **미사용 주제가 없으면 발행을 중단**하도록 코드로 수정했다.

원칙:
- **주제 1개 = 글 1개.** 같은 주제 슬러그로는 다시 발행하지 않는다. 스크립트가 이를 강제하므로
  폴백 로직을 되살리지 않는다.
- 새 글을 내고 싶으면 `data/auto-post-topics.json`에 **새 주제를 추가**한다. 주제를 추가할 때는
  기존 글과 검색 의도가 겹치지 않는지 제목 기준으로 먼저 확인한다.
- 자동발행 글은 현행대로 `noindex,follow` + sitemap 제외를 유지한다. 색인 대상 글은 사람이 검수한
  articles/·news/ 글로만 한정한다.
- **RSS(`feed.xml`)에도 자동발행 글을 넣지 않는다.** 피드는 `scripts/build-feed.mjs`가 색인 대상
  편집 글에서만 생성하며, 자동발행 스크립트는 피드를 건드리지 않는다(2026-09-01 분리).
- **애드센스 심사 기간 중에는 자동발행을 중단**하는 것을 기본으로 한다(스크립트를 돌리지 않으면 됨).
  심사 중 콘텐츠 추가는 사람이 작성·검수한 글로만 한다.
- 자동발행 글이 쌓이면 분기마다 한 번 아래 명령으로 중복 여부를 점검한다:
  본문 텍스트에서 날짜를 제거한 해시가 겹치는 글이 있으면 최신 1개만 남기고 삭제하고,
  `data/auto-post-log.json`과 `feed.xml`을 함께 동기화한다.

## 3. 템플릿 반복 금지 (재발 방지)

2026-07-14에 제거한 패턴을 다시 만들지 않는다:
- 페이지 제목을 그대로 반복하는 마무리 소제목("○○ 확인을 마무리하는 기준") 금지.
- 어느 글에 붙여도 성립하는 범용 블록(동일 문단 + 동일 표) 금지. 글에 넣는 표는
  그 글에서만 성립하는 내용(요금, 시설명, 일정)일 때만 쓴다.
- 새 지역 페이지를 추가할 때는 기존 5개 지역처럼 **지역마다 다른 각도**(운영 체계, 요금 구조,
  실측 사례 등)로 구성하고, 해당 지역에서만 유효한 구체 정보를 최소 3가지 포함한다.

### 3-1. 자동 발행 템플릿 (2026-08-29 정리)

애드센스가 "가치가 별로 없는 콘텐츠"로 거절한 뒤 확인한 결과, 자동 발행 글 12개가
**바이트 단위로 같은 "방문 전 빠른 점검표"** 를 달고 있었다. 생성 스크립트가 고정 표를
찍어냈기 때문이다. 조치와 이후 규칙:

- 12개 글의 점검표를 각 주제에 맞는 표로 교체했고, 본문도 1,500자 이상으로 보강했다.
- `scripts/auto-add-post.mjs`의 `renderTable()`은 이제 주제 데이터의 `checklist`에서만
  표를 만든다. `checklist`가 없으면 표를 아예 넣지 않는다.
- 모든 글에 붙던 범용 섹션 3개(검색 결과만 보고 이동하지 않는 이유 / 전화 확인을 짧게
  끝내는 방법 / TOWN114가 보는 좋은 정보의 기준)를 템플릿에서 제거했다.
- **새 주제를 추가할 때는 `sections`와 `checklist`를 그 주제에서만 성립하는 내용으로
  채운다.** 공통 문단을 템플릿에 되살리지 않는다.

## 4. 표와 모바일 가독성

2026-09-01에 본문 표 95개를 `.table-scroll` 컨테이너로 감쌌다. 이전에는 `.check-table`에 모바일 규칙이 없어
좁은 화면에서 열이 눌리고, `body { overflow-x: hidden }` 때문에 넘친 내용이 잘려도 스크롤할 수 없었다.

원칙:
- 본문에 표를 넣을 때는 반드시 다음 구조를 쓴다.
  `<div class="table-scroll" role="region" aria-label="표 가로 스크롤" tabindex="0"><table class="check-table">…</table></div>`
- 640px 이하에서는 표 최소 너비 560px가 적용되어 가로 스크롤로 읽는다. 열이 6개를 넘으면 표를 나누거나
  카드형 목록으로 바꾸는 편이 낫다.
- `npm run approval:audit`의 `table-scroll` 검사가 누락을 잡는다.

## 5. 지도·길찾기 링크

2026-09-01에 서비스 페이지 7개의 길찾기 링크 184개를 Google 지도에서 카카오맵으로 옮겼다.
**Google 지도는 국내 자동차 경로를 제공하지 않는다.** 모바일에서는 지도 앱이 링크를 가로채
정상처럼 보이지만 데스크톱에서는 경로가 열리지 않아, 오래 남아 있어도 발견되기 어려운 문제였다.

원칙:
- 길찾기 링크는 국내 지도 서비스를 쓴다. 좌표와 이름이 있으면
  `https://map.kakao.com/link/to/{이름},{위도},{경도}`, 검색어만 있으면
  `https://map.kakao.com/link/search/{검색어}` 형식을 쓴다.
- 위치를 보여주기만 하는 링크와 지도 임베드는 Google에서도 정상 동작하므로 그대로 두어도 된다.
  다만 **버튼 문구가 "길찾기"라면 반드시 국내 지도 서비스로 보낸다.**
- `npm run approval:audit`의 `google-maps-directions` 검사가 재발을 막는다.
- 2026-09-01 운영자가 실제 휴대폰에서 카카오맵 링크로 자동차 경로가 열리는 것을 확인했다.

## 6. 발행 전 최소 점검

새 글 커밋 전 확인:
- [ ] 제목이 기존 글과 검색 의도가 겹치지 않는가
- [ ] 본문에 이 글에서만 성립하는 구체 정보(수치·시설명·일정·기준일)가 있는가
- [ ] title/meta description이 본문과 일치하는가
- [ ] 내부 링크가 실제 존재하는 페이지를 가리키는가
- [ ] 이미지·다이어그램을 넣었다면 자체 제작인가, aria-label이 있는가
- [ ] 본문 내부 링크가 3개 이상인가
- [ ] 표를 `.table-scroll`로 감쌌는가
- [ ] `npm run seo:audit`와 `npm run approval:audit`가 통과하는가

승인 신청 전 판단 기준과 거절 대응 순서는 `ADSENSE_APPROVAL_GUIDE.md`를 따른다.
