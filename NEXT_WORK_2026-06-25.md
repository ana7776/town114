# 2026-06-25 이어서 할 일

## 1. 최신 파일 배포

오늘 로컬에서 수정한 내용이 아직 `https://town114.com/` 프로덕션에 모두 반영되지 않았습니다. 먼저 Cloudflare Pages 또는 현재 배포 경로에서 최신 파일을 배포합니다.

배포 후 확인할 URL:

```text
https://town114.com/sitemap.xml
https://town114.com/news/list/
https://town114.com/tools/visit-question-builder/
https://town114.com/sources/
https://town114.com/data/public-data-summary.json
```

## 2. Search Console 사이트맵 오류 정리

스크린샷의 `town114.com` 항목은 사이트맵 URL을 잘못 제출한 항목으로 보입니다.

진행 순서:

1. Google Search Console > Sitemaps에서 `town114.com` 실패 항목 삭제
2. 아래 주소만 새로 제출

```text
https://town114.com/sitemap.xml
```

3. 상태가 `성공` 또는 `가져옴`으로 바뀌는지 확인

## 3. 색인 신청 이어서

2순위까지 할당량 초과로 중단했으므로, 다음은 3순위부터 이어갑니다.

시작 URL:

```text
https://town114.com/news/auto-posts/2026-06-23-afternoon-library-holiday-notice-check/
```

전체 순서는 `INDEX_REQUEST_PRIORITY_2026-06-24.md`를 기준으로 진행합니다.

## 4. 공공데이터 추가 API 입력

현재 정상 수집 완료:

- 주차장
- 도서관
- 자동차 정비소
- 렌터카
- 약국

다음에 `.env.local`에 추가할 후보:

- `CIVIL_KIOSK_API_URL`
- `CULTURE_EVENT_API_URL`
- `CULTURE_FACILITY_API_URL`
- `EV_CHARGER_API_URL`
- `CHILDCARE_API_URL`
- `WELFARE_FACILITY_API_URL`
- `WORKNET_JOB_API_URL`

추가 후 실행:

```bash
node scripts/check-public-data-env.mjs
node scripts/fetch-public-data.mjs
node scripts/build-public-data-summary.mjs
```

## 5. 배포 전 로컬 검증 명령

```bash
node --check scripts/auto-add-post.mjs
node scripts/build-public-data-summary.mjs
node -e "JSON.parse(require('fs').readFileSync('data/public-data-summary.json','utf8')); JSON.parse(require('fs').readFileSync('data/sample-local-facilities.json','utf8')); console.log('json ok')"
```
